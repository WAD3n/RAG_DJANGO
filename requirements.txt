django>=4.2
djangorestframework>=3.14
boto3>=1.34
kafka-python>=2.0
docling>=2.0
docling-core>=2.0
# transformers 5.x requires torch>=2.7 (float8_e8m0fnu); pin to 4.x for torch 2.6 compat
transformers>=4.40,<5.0
easyocr
# PyTorch is installed separately in Dockerfile (CUDA build).
# Do NOT add torch here — it would overwrite the CUDA build with a CPU build.
pydantic>=2.0
pydantic-settings>=2.0
typer>=0.9
httpx>=0.25
openai>=1.30
rapidocr-onnxruntime
onnxruntime
django-prometheus>=0.3
# RAG / vector store
psycopg2-binary>=2.9
pgvector>=0.3
sentence-transformers>=3.0
